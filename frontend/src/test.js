import React from 'react';
import { Link } from 'react-router-dom';

let delta = () => {
    return(
        <div>
            I am a test page
            <Link to='/' > Home Page</Link>
        </div>
    )
}

export default delta